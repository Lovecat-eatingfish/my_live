# 复制为 .env 后按需修改；.env 不入库（已 gitignore）
# 作用域：docker-compose-full.yml 的 im-core-server（env_file 注入）

# 可选：把 im-core-server 的 Dubbo 注册地址 / Netty 寻址地址固定为指定 IP。
# 仅特殊网络拓扑需要（如 im-router 部署在容器网络之外、需要经宿主机访问 im-core 时，
# 需同时把 30035 端口映射出来）。纯容器化部署保持注释（容器自动探测即正确值）。
#
# 注意：Dubbo 3.2 拒绝回环地址（127.0.0.1），请填真实网卡 IP。
#
# DUBBO_IP_TO_REGISTRY=192.168.31.252
# DUBBO_PORT_TO_REGISTRY=30035
