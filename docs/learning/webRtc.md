可以简单理解为“先找路，再通话”：

信令协商：双方通过一个信令服务器（通常用 WebSocket）交换“我支持什么编码、我的网络地址是什么”等信息。WebRTC 标准不规定信令怎么走，留给开发者自己实现。

打洞与连接：利用 ICE/STUN/TURN 这一套机制，尝试在复杂的网络环境（NAT、防火墙）下，帮双方建立一条直接的点对点通道。实在打不通，才会用 TURN 服务器中转。

安全传输：通道建立后，音视频数据用 SRTP 加密，数据通道用 DTLS 加密，全程强制安全